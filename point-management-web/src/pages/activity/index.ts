import { lazy } from "react";

// ==============================|| ACTIVITY MANAGEMENT PAGES ||============================== //

export const ActivityListPage = lazy(() => import("./list"));
export const ActivityFormPage = lazy(() => import("./ActivityFormPage"));
